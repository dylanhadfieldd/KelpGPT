# Kelp RAG MVP

This is a minimum viable Retrieval-Augmented Generation system for kelp research.

To use:
- Add documents to `data/`
- Run `ingest.py` to index them
- Use `app/app.py` to query the system
