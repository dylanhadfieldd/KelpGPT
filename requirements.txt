# --- Core app ---
streamlit>=1.35
python-dotenv>=1.0

# --- OpenAI + LangChain ---
openai>=1.40
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.2

# --- Vector DB ---
chromadb>=0.5.5
# SQLite fix for Chroma (only for Python < 3.13 where sqlite3 is missing FTS5/JSON1)
pysqlite3-binary; python_version < "3.13"

# --- PDF & doc parsing ---
pypdf>=4.2
pdfplumber>=0.11
pdfminer.six>=20240706
python-docx>=1.1
pandas>=2.2

# --- Images (figures/thumbnails) ---
Pillow>=10.4
opencv-python-headless>=4.10

# --- Optional OCR (later) ---
pytesseract>=0.3.10

# --- Utilities ---
numpy>=1.26
tqdm>=4.66
rich>=13.7

# --- Optional local embeddings fallback ---
sentence-transformers>=3.0
